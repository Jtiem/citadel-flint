# Review: P0+P1c (acd05ca) — Mutation Planner + Tailwind Version Drift

**Reviewer:** flint-quality-gate | **Date:** 2026-04-11 | **Verdict: FIX (1 warning, 0 blockers)**

## TSC + Tests
- TSC: 0 errors
- MCP: 4440/4440 passing (new: mutationPlanner 16, tailwindVersionDrift 13)

## Checklist Results

| Check | Status |
|-------|--------|
| C3 Fresh Parse | PASS — planner reads violations, does not mutate AST |
| C7 ID Preservation | PASS — no structural ops |
| C12 Atomic Queuing | PASS — `tailwindVersionResolver.ts` uses `fs.readFileSync` for read-only config lookup (acceptable for MCP engine, not `src/`) |
| C13 No Regex Surgery | PASS — regex used only for class name *detection* (OPACITY_MODIFIER_RE, COLOR_CLASS_RE), not source code mutation |
| C15 Granular Tools | PASS — planner emits `fixToken`/`updateProp` op descriptors, not raw code |
| C16 TSC Loop | N/A — planner classifies, does not generate code |
| Process Boundary | PASS — `fs`/`path` imports are in `flint-mcp/`, not `src/` |
| Mithril Safety | PASS — no hardcoded hex in UI |

## WARNING: Missing `tailwind-version-drift` in mutationPlanner

`DETERMINISTIC_DRIFT_TYPES` does not include `tailwind-version-drift`. TW violations (which have `fixable: true` and a known replacement) fall through to the generic semantic fallback with confidence 0.3. These are deterministic map lookups and should classify as deterministic. Add `tailwind-version-drift` handling or include it in the drift set.

## Strengths
- Clean three-bucket architecture (deterministic/semantic/riskGated) with MRS integration
- Opacity modifier merge (bg-opacity-50 + bg-blue-500 -> bg-blue-500/50) is well-tested
- Identity-transform filter prevents false positives on classes like `sr-only`
- 29 new tests covering edge cases, empty input, threshold overrides, mixed batches
- Error taxonomy entries with clear recovery guidance

## Summary
Solid foundation. One warning: TW version drift violations misclassify as semantic instead of deterministic. Fix that, then SHIP.
