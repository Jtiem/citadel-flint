# Review — P2.5 Composition Validator + P3.5 Drift Trending
Commit: 687a109 — 2026-04-12

## Grade: SHIP (with 2 minor WARNINGs)

## Commandment Compliance
- **C13 (No Regex Surgery):** PASS. `compositionValidator.ts` uses `@babel/traverse` JSXElement enter/exit with a parent stack; no source-string manipulation.
- **C15 (AST Catalog):** PASS. Validator is read-only; emits LinterWarnings, no raw code emission. Mutation Planner correctly classifies composition as `semantic` with `fixable: false` — Flint diagnoses but does not auto-mutate, which is correct since restructuring requires intent.
- **C7 (ID Preservation):** N/A — read-only analyzer.
- **C3 (Fresh Parse):** N/A — accepts `File` AST from caller.

## SQL Correctness (driftTrendService)
- Parameterized queries throughout (`?` placeholders); no string interpolation into SQL. PASS.
- `tableExists` guards on every read — service degrades gracefully when `governance_events` / `mutations_ledger` are absent. PASS.
- `isoWeekStart` uses UTC day math correctly; Sunday (`getUTCDay()===0`) maps to +6 back to Monday. Correct.
- `getRepeatOffenders` uses `HAVING COUNT(*) >= ?` — correct aggregate filter.
- **WARNING (SQL):** `getFixRate.percentage` rounds to 2 decimals but doesn't clamp — if `autoFixed > total` (possible when auto-fix mutations outlive their originating violation window), percentage exceeds 100. Low risk, cosmetic.

## MCP Registration
- Tool listed in `ListTools` handler with full schema (required `projectRoot`, optional tuning params). PASS.
- Resource `flint://governance/trends` listed and resolved in `ReadResource` handler with empty-trend fallback on throw. PASS.
- `drift_regression` added to `AnomalyType` union in `governance/types.ts`. PASS. However — the commit message says "drift_regression anomaly type added" but `driftTrendService` emits alerts as plain strings, not through `AnomalyDetectionService.record()`. The new union member is declared but never produced. **WARNING:** dead enum branch until Flare integration lands.

## Edge Cases
- Empty registry → validator short-circuits. PASS.
- Non-PascalCase JSX (intrinsics) skipped. PASS.
- `DEFAULT_COMPOSITION_RULES` seeded and merged with registry overrides correctly (spread ordering).
- `buildRulesMap` merge uses shallow spread — registry `compositionRules` overrides defaults field-by-field. Correct.
- Depth tracking decrements in `exit` — balanced with enter. PASS.
- **Minor:** parent stack pops only if top matches current name — defensive but masks mismatched traversal. Acceptable.

## Test Coverage
- `compositionValidator.test.ts` (388 LOC) and `driftTrendService.test.ts` (448 LOC). 28 new tests reported, MCP 4557/4557, TSC 0. Accepted.

## Policy Engine
- New rule IDs added to `KNOWN_MITHRIL_RULES` — policy overrides will apply. PASS.
- Error taxonomy entries FLINT-MITH-020/021/022 complete with explanations + recovery. PASS.

## Verdict
Clean, well-scoped, tests in place. Ship it. Address the two WARNINGs in a follow-up: (1) clamp fixRate percentage at 100, (2) wire `drift_regression` alerts through `AnomalyDetectionService` so the new enum value is actually emitted.
