# P2.8 Review -- D2C Refinement Governance Gate

**Commit:** 566e6a4  
**Reviewer:** flint-reviewer  
**Date:** 2026-04-11  
**Verdict:** FIX (1 BLOCKER, 2 WARNINGs)

---

## BLOCKER

**C13 -- No Regex Surgery (line 596, d2cRefinement.ts)**

```
finalCode = finalCode.replace(violatingPattern, targetValue)
```

`extractViolatingValue` parses a linter message with regex, then `String.replace` performs source code modification. This is textbook Commandment 13. The comment on line 583 acknowledges "full AST mutation goes through fix.ts" but then does regex surgery anyway. The fix: call `fix.ts` (the existing deterministic fixer) instead of inline replace, or defer auto-fix to the caller and only report what needs fixing.

## WARNINGs

1. **Weak degradation comparison (line 550).** Counts total violations but ignores severity. Refined code with 1 critical + 0 amber beats scaffold with 0 critical + 2 amber under this logic. Weight by severity or at minimum compare critical counts separately.

2. **Conditional test assertions (tests 3, 4).** Tests 3 and 4 wrap their key `expect` calls in `if` blocks, meaning they pass vacuously when the condition is false. These should use deterministic fixtures that guarantee the branch is hit, or `expect.assertions(N)` to enforce the assertion fires.

## Approved items

- GovernanceScore type contract: clean, well-typed, no `any`.
- Graceful degradation on every external call (try/catch with skip): correct.
- Risk scoring integration via dynamic import: appropriate for MCP engine context.
- Ledger recording is best-effort with proper db.close(): correct.
- 11/11 tests pass. TSC clean.
- No process boundary violations (this is flint-mcp, Node.js is allowed).

## Required action

Remove the inline `finalCode.replace(...)` block (lines 582-599). Either route through `fix.ts` or report `autoFixed` as a count-only field and let the caller apply fixes through the AST pipeline.
