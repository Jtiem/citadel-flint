# P3 + P4 Review — commit 080c350

**Date:** 2026-04-12
**Scope:** Domain escalation (healthcare/fintech/government), typography hierarchy visitor, team registry overlays, HYDRATION-001 linter
**Verification:** MCP 4596/4596 passing, TSC 0 errors

## Grade: SHIP

## Commandment Compliance

- **C13 Deterministic Surgery:** hydrationLinter uses Babel `parse` + `traverse` on JSXText / JSXAttribute / JSXExpressionContainer. Regex is applied only to *literal string values already extracted from the AST*, never to raw source. Compliant.
- **C15 Granular AST Tools:** HYDRATION-001 correctly marked `fixable: false` and routed as `semantic` in mutationPlanner — Flint flags, never guesses prop names. Correct.
- **C2 No Hallucinated Styling:** Domain escalations tighten, never relax (`Math.min` for deltaE, `'off'` preserved). Correct.

## Policy Engine Integration

- `applyDomainEscalation` called at end of `coerceToResolved` — good single entry point.
- `KNOWN_MITHRIL_RULES` extended with `MITHRIL-TYP-HIERARCHY`, `MITHRIL-SPC-TOUCH`, `HYDRATION-001`. Correct.
- Unknown domain falls through `default:` unchanged. Safe.
- `minTouchTargetPx` is declared in the policy interface but no visitor consumes `MITHRIL-SPC-TOUCH` yet — fintech escalation wires a rule ID that has no implementing visitor in this commit. **WARNING:** dead rule until the touch-target visitor lands; will silently never fire.

## Type Safety

- `LinterWarning.type` union extended with `'hydration'`. Clean.
- Government overlay adds `section508?: boolean` to `PolicyA11ySection` — typed.
- No `any`, no `@ts-ignore`. Clean.

## Edge Cases

- **Heading skip:** Visitor tracks `maxSeenRank` and advances after flag, preventing cascading re-flags on deeper headings. Correct handling for `h1→h3→h5` (two flags expected). Sibling headings at same level pass.
- **Unknown domain:** Returns policy unchanged.
- **Hydration false positives:** The `$NN.NN` regex `/\$\s?\d{1,3}(,\d{3})*\.\d{2}\b/` will fire on legitimate currency display strings that a designer legitimately hardcoded (e.g. "$0.00" in an empty state). **WARNING:** consider excluding `$0.00` and documenting an opt-out path via `placeholderPatterns` override or per-file ignore. Severity defaults to `amber` not `critical`, so this is tolerable.
- **JSXText whitespace:** `raw.trim().length === 0` guard in `literalMatches` prevents whitespace-only flags. Good.
- **Parse failure:** `detectHydrationViolations` swallows parse errors and returns `[]`, delegating to MithrilLinter's own error channel. Correct.
- **Heading from props `<Heading level={n}>`:** Correctly ignored (only `JSXIdentifier` intrinsics h1–h6 walked).

## Test Coverage

317 lines of P3 tests + 186 lines of hydration tests, 39 new tests. Domain escalation, team overlay merge, hierarchy skipping, Figma hint matching, pattern detection all covered. Adequate.

## Warnings (non-blocking)

1. `MITHRIL-SPC-TOUCH` has no visitor yet — fintech touch-target rule is currently a no-op. File-level TODO or follow-up task should be tracked.
2. Currency regex may false-positive on legitimate `$0.00`/`$N.NN` display literals. Document override path in rule explanation.
3. `hydrationLinter` re-parses the source independently from the AST already traversed by MithrilLinter — acceptable (comment documents why), but costs a second parse per file.

## Summary

Clean, well-scoped, Commandment-compliant. Ships.
