import clsx from 'clsx'
export const C = ({ size }: { size: string }) => (
  <div className={clsx('inline-flex items-center justify-center', 'whitespace-nowrap rounded-md text-sm font-medium')}>
    I
  </div>
)
