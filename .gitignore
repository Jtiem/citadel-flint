node_modules
dist-electron
dist
dist-server
dist-web/
release/

# OS files
.DS_Store
**/.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.*

# Logs
*.log

# Databases (local runtime)
*.db
*.sqlite

# Debug artifacts
test-root.js
test-main-cjs.cjs

# TypeScript build cache
*.tsbuildinfo

# Compiled shared module output (generated from shared/*.ts)
shared/*.js
shared/*.js.map
shared/*.d.ts
shared/*.d.ts.map
# Hand-written ambient declarations live in shared/ too — exempt them from
# the auto-generated-output ignore. Add new ambient .d.ts files here as needed.
!shared/babel-plugins.d.ts

# Flint runtime event logs
.flint/mcp-events.jsonl
.flint/mcp-events.jsonl.bak
**/.flint/telemetry-events.jsonl
**/.flint/telemetry-events.jsonl.bak
.flint/ide-active-file.json
.flint/context.json

# Storybook (stale template — revive or delete later)
src/stories/

# SQLite WAL/SHM (runtime)
*.db-shm
*.db-wal

# Test output artifacts
test-results/
**/playwright-report/

# Flint runtime context (written every 200ms)
.flint/context.json
